# Database configuration
DB_CONTAINER_NAME=tia-development-database
DB_USER=root
DB_PASS=your_database_password_here
DB_HOST=localhost
DB_NAME=tiapartners
DB_PORT=3306

# Application environment
NODE_ENV=development
PORT=5000

# Frontend base URL for CORS and Swagger
FRONTEND_BASE_URL=http://localhost:3000

# JWT secret for authentication (use a strong, unique secret in production)
JWT_SECRET=your_jwt_secret_here

# ADK API configuration
ADK_API_URL=http://localhost:8080/chat
ADK_RECORD_TEST=false

# GNN API URL
GNN_API_URL=http://your-gnn-api-url-here/api/v1