export const sendMessage = async (req, res) => {
    try {

    } catch (error) {
        console.error('', error);
        res.status(500).json({ success: false, message: error.message || 'Failed to process request' });
    }
}

export const conversationHistory = async (req, res) => {
    try {

    } catch (error) {
        console.error('', error);
        res.status(500).json({ success: false, message: error.message || 'Failed to process request' });
    }
}

export const markAsRead = async (req, res) => {
    try {

    } catch (error) {
        console.error('', error);
        res.status(500).json({ success: false, message: error.message || 'Failed to process request' });
    }
}

export const typingIndicator = async (req, res) => {
    try {

    } catch (error) {
        console.error('', error);
        res.status(500).json({ success: false, message: error.message || 'Failed to process request' });
    }
}

export const conversationSession = async (req, res) => {
    try {

    } catch (error) {
        console.error('', error);
        res.status(500).json({ success: false, message: error.message || 'Failed to process request' });
    }
}

